# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Math and ML libraries
numpy==1.24.3
scikit-learn==1.3.2

# Embedding model (installs torch and transformers as dependencies automatically)
sentence-transformers==2.2.2
# torch==2.1.0  # Not needed - installed automatically by sentence-transformers
# transformers==4.35.2  # Not needed - installed automatically by sentence-transformers

# Homomorphic encryption (optional - has fallback if not installed)
# tenseal  # Commented out - using simulated HE for now (saves space)

