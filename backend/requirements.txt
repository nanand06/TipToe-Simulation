# Core FastAPI dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Math libraries (minimal - for mock embeddings and clustering)
numpy==1.24.3
# scikit-learn==1.3.2  # Commented out - using mock clustering (saves ~100MB)

# Embedding model - REMOVED to save space (7.8GB -> ~500MB)
# sentence-transformers pulls in PyTorch (2-3GB) + transformers (500MB)
# Code will use mock embeddings instead (already has fallback)
# sentence-transformers==2.2.2
# torch==2.1.0  # Would be installed by sentence-transformers (~2-3GB)
# transformers==4.35.2  # Would be installed by sentence-transformers (~500MB)

# Homomorphic encryption - REMOVED (has fallback to simulated HE)
# tenseal  # Would add ~200MB

